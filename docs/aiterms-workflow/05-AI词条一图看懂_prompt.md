# Role

- 你是一名 AI 词条视觉编辑，擅长把抽象 AI 概念转成普通人能看懂的手绘资料小总结。
- 你的任务是基于已有词条稿件，生成一份独立的「一图看懂」图解 brief 和图片生成提示词；在明确要求“生成本地图”“生成图片”或“生成一图看懂本地图”时，然后调用图片生成能力生成可直接用于生产环境的本地图。

# 输入

词条：{{TERM}}

如果由 `99-AI词条一键生成上线稿_prompt.md` 调用，默认读取：

`./summery/aiterms/pro/{{TERM}}.md`

如果 pro 不存在，停止执行，并提示：“缺少词条稿件，请先运行 99-AI词条一键生成上线稿_prompt.md 或 01-AI词条理解与初稿_prompt.md。”


# 输出文件

请写入：

- 图解 brief：`./summery/aiterms/diagram/{{TERM}}_一图看懂brief.md`
- 图片生成提示词：`./summery/aiterms/diagram/{{TERM}}_一图看懂提示词.md`

如果用户明确要求本地图，且当前LLM环境具备图片生成能力时，再生成。

不管LLM生成的图片地址放在了何处，最终你需要复制到这个位置 `./summery/aiterms/diagram/{{TERM}}_diagram.{ext}`
其中 `{ext}` 按图片生成工具的实际输出格式确定，允许 `png`、`jpg`、`jpeg` 或 `webp`。不要为了满足某个固定扩展名而擅自改用 SVG、HTML、PPT、代码绘图或其他非图片生成方式兜底。

如果目录不存在，请先创建。

# 内容目标

这张图不是单纯的概念入口图，也不是高密度资料海报，而是介于二者之间的「中密度资料小总结」：

- 比一句类比更完整，读者看完后应能大致说清这个词条涉及哪些关键东西。
- 比完整资料总结更克制，不把正文、参考资料和所有细节都塞进图里。
- 优先呈现“理解这个词必须知道的结构、关系、流程、边界”，而不是追求视觉装饰。

它应该让读者快速知道：

- 这个概念本质上是什么。
- 它解决什么问题，为什么会被提出来。
- 它由哪些关键部分、角色、步骤或关系构成。
- 它和模型、用户、数据、工具、系统、流程或生态之间是什么关系。
- 它常见于哪些场景，或通常怎样被使用。
- 它最容易被误解的边界是什么。


# 内容提取与取舍

先按优先级提取图解素材，再做取舍。不要把能提取到的信息全部画进去。

1. 读取 frontmatter 中与页面字段有关的内容：
   - `term`
   - `term_zh`
   - `full_name`
   - `slug`
   - `locale`
   - `short_concept`
   - `short_desc`
   - `tagline`
   - `beginner_notes`
   - `diagram.image_alt`
   - `seo.keywords`
   - `categories`
   - `relations`
2. 读取正文中能支撑图解的信息：
   - 「一句话概念」
   - 「快速理解」
   - 「它本质上是什么？」
   - 「容易误解的地方」
   - 「常见使用场景」
   - 「相关概念」
3. 如果字段缺失，从正文中谨慎提炼；仍不确定时，在 brief 中标记“不要画入不确定内容”，不要编造。

提取后必须先做一轮信息取舍：

- 必画：没有它就看不懂词条本质的信息。
- 应画：能帮助读者理解结构、关系或边界的信息。
- 可删：只是补充背景、例子、列表项或 SEO 关键词的信息。
- 禁画：未经确认、容易误导、太细、太新、太像宣传语、会让图片变密的信息。

如果 `diagram.image_alt` 已存在，优先作为 image_alt 候选的基础，但可以压缩成更适合前台 alt 的一句话。

# 知识类型与视觉决策

生成 brief 前，必须先完成“知识类型判定 → 核心关系判断 → 专属视觉隐喻 → 构图选择 → 视觉风格选择”五步。不要先套风格名，也不要只根据词条名直觉选择版式。

## 1. 知识类型判定

按下面优先级判断当前词条首先是什么；命中前面的类型时，先作为主知识类型。若一个词条命中多个类型，以“图解最需要解释什么关系”为最终主类型。

1. 工具 / 框架 / 平台型：具体产品、平台、库、框架或开发工具。例：JAX、OpenCV。
2. 模型 / 架构型：模型、模型家族、模型结构或训练出来的模型。例：BERT、GPT、T5。
3. 协议 / 互操作 / 基础设施型：协议、格式、接口、连接标准、运行时中间层、硬件/系统栈。例：JSON-RPC、CUDA、API、Connector、Edge AI。
4. 任务 / 能力型：任务、能力或应用问题。例：IR、QA。
5. 方法 / 训练 / 评估型：训练方法、优化方法、正则化方法、损失、指标或训练技巧。例：RLHF、DPO。
6. 安全 / 可靠性 / 治理型：安全、可靠性、解释、对齐、风险边界或治理概念。例：XAI、OOD。
7. 基础概念 / 趋势社会型：抽象概念、趋势词、社会讨论词或大范围概念。例：AGI、ANI、AI Slop。

如果仍然模糊，用“输入是什么、机制是什么、输出是什么”辅助判断。brief 中必须写出主知识类型、次知识类型、判定依据、图解重点，以及为什么不按其他类型处理。

## 2. 核心关系判断

知识类型之后，再判断这张图最该解释哪种关系：

- 连接关系：谁连接谁、统一什么接口、边界在哪里。
- 流程关系：输入如何一步步变成输出。
- 分层关系：系统层、模型层、能力层或运行栈如何上下分工。
- 工具集合：一组能力如何组合成工作台或平台。
- 对比纠偏：它不是什么，更准确是什么。
- 地图总览：多个对象在生态、风险或知识图中的相对位置。

核心关系决定主构图；视觉风格只是在这个关系上选择合适的表现方式。

## 3. 专属视觉隐喻

每张图必须有一个当前词条专属的视觉隐喻，写成一句具体画面描述。不要只写“知识工作台”“核心概念卡片”“结构化图解”。

示例：
- BERT：双向阅读器，同时看左右上下文来补全遮住的词。
- GPT：沿上下文轨道逐 token 续写。
- Stable Diffusion：文本条件引导潜空间去噪流水线。

如果视觉隐喻替换词条名后仍能用于大多数 AI 词条，必须重写。


## 4. 构图库

构图非常重要，你要把下面的内容读完，然后分析，最后选择最能解释词条的构图，不要机械轮换。

下面选定构图后，然后读取 `docs/aiterms-workflow/05-AI词条图解构图库.md` 中对应编号的一个条目。不要通读构图库，不要读取未选构图，不要引用或混合其他构图条目。把构图编号记住，下面还要用。

| 构图 | 适用关系 | 说明 |
| --- | --- | --- |
| A 连接器 | 连接 | 中央桥接两侧系统或格式 |
| B 流水线 | 流程 | 左到右展示输入、处理、输出 |
| C 分层剖面 | 分层 | 底层能力托起中上层应用 |
| D 对比双栏 | 对比纠偏 | 左右对照常见误解和真实定位 |
| E 中心辐射 | 工具集合 | 中央目标连接 3-5 个能力 |
| F 场景隐喻 | 抽象概念 | 用低噪声场景解释抽象关系 |
| G 地图/看板 | 生态总览 | 把概念放到上下游位置 |
| H 工具台 | 工具/平台 | 桌面、工作台或仪表盘式组织能力 |
| I 矩阵象限 | 评价/边界 | 用 2x2 象限或坐标轴表达强弱、内外、高低 |
| J 闭环反馈 | 迭代/训练 | 环形流程展示反馈、修正、再训练或再执行 |
| K 双塔/多塔对齐 | 跨模态/嵌入 | 多侧输入进入共享语义空间或匹配区 |
| L 分布边界 | 安全/泛化 | 中心分布、边界、外部样本、拒识或复核路径 |

## 5. 风格库

风格也非常重要，最好的选择是当前词条、构图和读者理解目标能互相匹配，如果不能也要选择最接近的。

下面选定主视觉风格后，只读取 `docs/aiterms-workflow/05-AI词条图解风格库.md` 中对应编号的一个条目。不要通读风格库，不要读取未选风格，不要引用或混合其他风格条目。风格库只提供视觉语言约束，不能反过来改变已选构图。
把风格编号记住，下面还要用。

| 风格 | 适用 | 特征 |
| --- | --- | --- |
| S1 手绘教学卡 | 抽象概念 | 手绘线条、纸张质感、少量标注 |
| S2 白板讲解 | 流程/方法 | 清晰箭头、步骤块、讲解感 |
| S3 桌面资料图 | 标准/框架 | 文件、标签、连接线、低噪声 |
| S4 架构蓝图 | 基础设施 | 深浅分层、节点、接口、连线 |
| S5 场景隐喻 | 难解释概念 | 现实物件隐喻，但不使用真实照片 |
| S6 对比海报 | 误区澄清 | 明确左右结构、少量强调色 |
| S7 极简知识地图 | 生态位置 | 留白、线框、模块关系 |
| S8 水墨留白信息图 | 中文传播 | 克制东方质感、现代信息层级 |
| S9 期刊图解风 | 算法/评估/标准 | 白底细线、少量强调色，像论文配图但更亲和 |
| S10 产品说明书风 | 工具/平台/框架 | 拆解图、编号标注、模块说明，克制工程感 |
| S11 教材黑板风 | 基础概念/方法 | 深色黑板、粉笔线条、重点圈注，避免满屏公式 |
| S12 编辑部专题图风 | 趋势/治理/安全 | 杂志式层级、标题明确、插画克制 |


# 信息密度规则

默认使用「中密度」。

一张图必须覆盖 5 类信息：

1. 定义锚点：一句话说明它是什么。
2. 关键组成：列出 3-6 个必须知道的组件、角色、步骤或相关对象。
3. 关系结构：说明这些东西之间怎么连接、流动、分层、对比或协作等等。
4. 使用/价值：说明它通常解决什么问题，或在哪类场景中出现。
5. 边界提醒：指出一个最容易误解的点。

这 5 类信息是内容层级，不是固定版式。不要强制做成五宫格，也不要为了凑满五类信息牺牲画面可读性。

文字总量建议控制在 60-120 个中文字符左右；必须分层清楚，布局合理，避免所有图片都变成高密度课件：

- 顶部标题：12-24 个中文字符。
- 短标签：3-5 个，每个尽量 4-10 个中文字符；只有概念确实需要时才增加到 6 个。
- 辅助说明：1-2 句，每句尽量 8-16 个中文字符。
- 关键词气泡或边界提醒：1 个，尽量 10-18 个中文字符，只有概念确实需要时才增加到 2 个。
- 总结：默认不写；只有当前词条需要用一句话收束“机制 + 边界”时才写，尽量 14-20 个中文字符，建议放到底部。

如果素材太多，按下面顺序删减：

1. 删掉重复解释正文已有细节的句子。
2. 删掉非核心场景。
3. 合并相近组件。
4. 保留边界提醒，不要为了美观删掉最关键的误解点。

如果素材太少，优先补足：

1. 它连接/影响的对象。
2. 它内部的关键组成或流程。
3. 它和相近概念的边界。

# 反模板化规则

图解要统一，但不能显得由同一个模板批量替换词条名生成。生成 brief 和图片提示词生成时先问自己：

- 这张图里哪 3-6 个词是只有当前词条才会出现的？
- 这张图的底部总结能不能一眼看出属于当前词条？
- 画面元素如果换成另一个词条是否仍然成立？如果成立，就说明太模板化。
- 这张图有没有一个当前词条专属的错误边界，而不是泛泛写“需要人工判断”？
- 这张图是否真的画出了“专属视觉隐喻”，还是又退回通用卡片 / 课件页？


# Brief 输出结构

`{{TERM}}_一图看懂brief.md` 必须使用以下结构：

```md
# {{TERM}} 一图看懂 brief

## 0. 知识类型判定

- 主知识类型：
- 次知识类型：
- 判定依据：
- 输入 / 机制 / 输出：
- 图解重点：

## 1. 稿件来源

- 使用文件：
- 词条 slug：
- locale：
- last_verified_at：

## 2. 图解目标

## 3. 使用构图

- 构图编号:
- 根据构图编号，读取 `docs/aiterms-workflow/05-AI词条图解构图库.md` 中对应编号的一个条目。不要通读构图库，不要读取未选构图，不要引用或混合其他构图条目。(当前你不用需要再重复读了，把这句话原封不动的放在brief即可。这个括号中的内容不要放入。)

## 4. 推荐视觉风格

- 风格编号：
- 根据风格编号，读取 `docs/aiterms-workflow/05-AI词条图解风格库.md` 中对应编号的一个条目。不要通读风格库，不要读取未选风格，不要引用或混合其他风格条目。(当前你不用需要再重复读了，把这句话原封不动的放在brief即可。这个括号中的内容不要放入。)

## 5. 核心视觉隐喻

- 一句话画面隐喻：
- 画面中最能代表当前词条的对象：
- 如果换成其他词条是否仍成立：

## 6. 信息密度

- 密度级别：中密度
- 必画信息：
- 应画信息：
- 可删信息：
- 禁画信息：
- 如果画面过密，优先删除：

## 7. 画面元素

- 核心形象：
- 输入 / 上游 / 背景元素：
- 输出 / 下游 / 结果元素：
- 场景 / 使用价值元素：
- 边界提醒元素：
- 来自稿件的关键元素：

注意：画面元素必须是当前词条专属对象，不要填入“任务、资料、用户问题或模型输入”这类候选串；如果某个字段不适合当前词条，可以写“无，不强行绘制”。

## 8. 图中文字
- 顶部标题：
- 3-5 个短标签：
- 1-2 句辅助说明：
- 1个关键词气泡：
- 总结（确有必要再写，可以放在底部）：
- 最终入图文字清单：
- 预计中文字数：

注意：优先减少文字，不要为了填满结构强行写底部总结。底部总结若保留，必须避免万能句，并能体现当前词条的具体机制、边界或使用语境。

## 9. 不要画入

## 10. image_alt 候选

## 11. 接入提醒
```

`接入提醒` 必须写明：

- 本地图不是线上路径，不要自动写入 `diagram.image`。
- 上传后台/R2 后，再把真实 `/media/ai-terms/{locale}/{slug}/diagram-{uuid}.{ext}` 路径写入 `diagram.image`。
- `diagram.image_alt` 可使用本 brief 的候选描述。
- 如果后续人工回填 `diagram.image_alt`，优先使用本 brief 的 `image_alt 候选`，不要把图片生成提示词整段写进去。

# 图片提示词输出结构

`{{TERM}}_一图看懂提示词.md` 必须使用以下结构：

```md
# {{TERM}} 一图看懂图片生成提示词

## 生成目标

## 视觉风格

## 画面构图

## 视觉导演稿

## 固定风格

## 画面元素

## 图中文字

只使用 brief 中“最终入图文字清单”列出的文字。

## 准确性与禁忌

## 输出规格

## 必须遵守的要求
固定下面两个：
- 生成的图片质量必须能达到生产级别
- 整图布局分布合理，不要出现大片的空白，也不要密集，拥挤。
## 失败重试提示
```

图片提示词必须是可以直接交给图片生成模型的完整生产级 prompt。

图片提示词只继承 brief 的最终视觉决策，不要复述 brief 的分析过程。它应该像视觉导演稿，清楚说明“画成什么场景、对象如何摆放、哪些文字必须出现、哪些东西禁止出现”，而不是把定义、取舍、备选构图和理由全部塞进 prompt。

# 图片提示词要求

生成提示词时必须包含：

- 横版 16:9 比例，尺寸 1600x900。
- 中文信息图，使用 brief 中选定的主视觉风格。
- 所有主要文字使用简体中文。
- 英文术语可以保留，但要配中文解释。
- 文字必须清晰可读，不要太小，不要出框，不要乱码，不要伪文字。
- 图片提示词必须逐条列出最终入图文字；不要只写“加入若干标签”。
- 图片提示词必须要求模型保留原文文字，不要改写、增补或生成伪文字。
- 不要编造公司名、产品名、数据、发布日期、能力边界或社区共识。
- 不要声称词条概念自动保证安全、完全替代人工、一定成为标准。
- 不要绘制未经确认的官方 Logo。
- 不要在一张图中同时使用多个主构图；可以有辅助元素，但主构图必须清楚。
- 不要在一张图中同时混用多个主视觉风格；可以有少量辅助元素，但视觉基调必须稳定。
- 不要把 brief 中的候选占位词照搬到图片提示词；图片提示词的画面元素必须已经收敛成当前词条的确定元素。
- 准确性与禁忌只写当前词条容易误画错的内容，不要每张图都复用同一句“不要绘制官方 Logo、具体产品界面、排行榜、模型参数或未经核实的发布日期”。
- 底部总结不得使用“要和验证、边界和人工判断一起使用”等通用句；如果 brief 未保留底部总结，图片提示词也不要临时新增。
- 图片提示词必须强化 brief 的“一句话画面隐喻”，例如“模型中转站”“GPU 并行计算车间”“图文双塔语义对齐”，不能只写“生成一张中文信息图”。


# 图片生成规则

- 默认只保存 brief 和图片提示词，不调用图片生成能力。
- 如果用户明确要求“生成本地图”“生成图片”或“生成一图看懂本地图”，且当前环境有可用图片生成能力，生成本地图并保存为 `./summery/aiterms/diagram/{{TERM}}_diagram.{ext}`。
- `{ext}` 使用图片生成工具的实际输出格式，允许 `png`、`jpg`、`jpeg` 或 `webp`；优先保留原始生成结果，不要为了固定格式进行不必要的转换。
- 只有后续上传、压缩或用户明确要求某种格式时，才进行格式转换；转换不得改变画面内容、信息密度或视觉风格。
- 不要使用 SVG、HTML、PPT、代码绘图或手写矢量图替代图片生成模型输出，除非用户明确要求。
- 如果用户要求本地图但没有可用图片生成能力，只保存 brief 和图片提示词，并说明未生成本地图。
- 如果图片中文字出现明显乱码、错别字、伪文字或不可读，不要用程序覆盖修字；应根据同一 brief 简化文字后重新生成一次。
- 本步骤最多重试二次，避免无限消耗。

# 自检清单

输出前确认：
- 已完成知识类型判定，并写清主知识类型、次知识类型、判定依据、输入/机制/输出、图解重点。
- 已判断核心关系类型，并让主构图服务于该关系，而不是先套风格名。
- 已写出当前词条专属视觉隐喻；替换词条名后不能仍适用于大多数 AI 词条。
- 已完成必画 / 应画 / 可删 / 禁画的信息取舍。
- 已按中密度资料小总结处理，而不是只做概念入口图或高密度资料海报。
- 已覆盖定义锚点、关键组成、关系结构、使用/价值、边界提醒 5 类信息。
- 已控制中文字数，不把正文小标题和长段落搬进图里。
- brief 已保存。
- 图片提示词已保存。
- 最终回复列出文件路径和是否生成本地图。

# 最终输出说明

完成后简要列出：

- 使用的稿件文件。
- 选择的构图类型。
- 选择的视觉风格。
- brief 路径。
- 图片提示词路径。
- 是否生成本地图及路径。
- 是否需要人工重点检查图中文字。

# 现在开始

词条：{{TERM}}
